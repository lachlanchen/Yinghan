[English](../README.md) · [العربية](README.ar.md) · [Español](README.es.md) · [Français](README.fr.md) · [日本語](README.ja.md) · [한국어](README.ko.md) · [Tiếng Việt](README.vi.md) · [中文 (简体)](README.zh-Hans.md) · [中文（繁體）](README.zh-Hant.md) · [Deutsch](README.de.md) · [Русский](README.ru.md)


[![LazyingArt banner](https://github.com/lachlanchen/lachlanchen/raw/main/figs/banner.png)](https://github.com/lachlanchen/lachlanchen/blob/main/figs/banner.png)

# تجزئة الأورغانويد (ويب + CLI)

![Python](https://img.shields.io/badge/Python-3.x-blue.svg)
![Framework](https://img.shields.io/badge/Backend-Tornado-009688.svg)
![AI](https://img.shields.io/badge/OpenAI-Vision%20Segmentation-412991.svg)
![Status](https://img.shields.io/badge/README-First%20Complete%20Draft-success.svg)
![Interface](https://img.shields.io/badge/UI-Web%20%2B%20CLI-0ea5e9)
![Outputs](https://img.shields.io/badge/Artifacts-Overlay%20%7C%20Mask%20%7C%20JSON-f97316)
![PWA](https://img.shields.io/badge/PWA-Minimal%20Support-22c55e)
![API](https://img.shields.io/badge/API-POST%20%2Fapi%2Fsegment-0f766e)
![Format](https://img.shields.io/badge/Result-Polygon%20JSON-f59e0b)

تطبيق Python لتجزئة الأورغانويد في صور المجهر باستخدام نماذج OpenAI الداعمة للرؤية.

> صُمم لتجارب محلية سريعة: ارفع الصورة مرة واحدة، وعاين نتائج الـ overlay/mask/JSON، ثم كرر مع اختيار نموذج مختلف.

يتضمن هذا المستودع:
- خادم ويب Tornado مع واجهة رفع.
- سير عمل CLI للاستخدام الدفعي أو البرمجي.
- استخراج المضلع، توليد القناع، وعرض صورة مشروحة.
- دعم PWA بسيط (manifest + تخزين service worker للأصول الثابتة الأساسية).

## 🧭 تنقّل سريع

| القسم | الغرض |
|---|---|
| [نظرة عامة](#-نظرة-عامة) | فهم ما الذي يفعله المشروع وما هي مخرجاته |
| [الميزات](#-الميزات) | الاطلاع على القدرات الأساسية في تدفقات الويب وCLI وAPI |
| [بنية المشروع](#️-بنية-المشروع) | العثور على الملفات الأساسية ومجلدات التشغيل |
| [المتطلبات المسبقة](#-المتطلبات-المسبقة) | التأكد من متطلبات البيئة |
| [التثبيت](#️-التثبيت) | إعداد بيئة Python والتبعيات |
| [الاستخدام](#-الاستخدام) | تشغيل تطبيق الويب أو CLI أو استدعاءات API المباشرة |
| [الإعداد](#️-الإعداد) | ضبط النموذج ومعلمات التشغيل |
| [الأمثلة](#-الأمثلة) | إعادة استخدام مقاطع CLI وPython |
| [ملاحظات التطوير](#-ملاحظات-التطوير) | فهم تفاصيل التنفيذ ونصائح العمل المحلي |
| [استكشاف الأخطاء وإصلاحها](#️-استكشاف-الأخطاء-وإصلاحها) | حل مشكلات التشغيل والنموذج الشائعة |
| [خريطة الطريق](#️-خريطة-الطريق) | التحسينات المخطط لها لاحقًا |
| [المساهمة](#-المساهمة) | إرسال التعديلات بطريقة فعّالة |
| [الدعم](#support) | خيارات التبرع |
| [الترخيص](#license) | حالة الترخيص الحالية |

## 🔍 نظرة عامة

يستقبل التطبيق صورة مجهرية كمدخل، ثم يرسلها إلى نموذج OpenAI مع موجه يعتمد مخطط JSON صارمًا، ويُرجع مضلعًا واحدًا يحدد حدود الأورغانويد.

### 🔄 سير العمل من البداية إلى النهاية

1. استلام الصورة عبر رفع الويب أو مسار CLI أو نموذج API بصيغة multipart.
2. استدعاء نموذج OpenAI لإنتاج خرج مضلع منظّم.
3. التحقق من إحداثيات المضلع وتقييدها ضمن حدود الصورة.
4. إنشاء وحفظ ثلاثة نواتج: صورة مشروحة، قناع ثنائي، JSON للمضلع.
5. إرجاع URLs/paths والبيانات الوصفية (`width`, `height`, `confidence`).

### 📌 لمحة سريعة

| المجال | التفاصيل |
|---|---|
| المدخل | صورة مجهرية |
| المخرج الأساسي | مضلع أورغانويد (نقاط `x, y`) |
| الملفات المشتقة | PNG overlay مشروح، PNG قناع ثنائي، JSON مضلع |
| طرق الوصول | واجهة ويب، CLI، استدعاء API مباشر |
| الواجهة الخلفية | Tornado (`server.py`) |
| مسار الذكاء الاصطناعي | OpenAI Responses API أولًا، وChat Completions كبديل |

النواتج المُولدة:
- `*_annotated.png`: الصورة المصدر مع overlay أحمر شبه شفاف.
- `*_mask.png`: قناع أورغانويد ثنائي.
- `*_polygon.json`: خرج منظّم (`width`, `height`, `polygon`, `confidence`).

ملفات التشغيل الأساسية:
- `server.py`: تطبيق الويب ومسارات API.
- `organoid_segmenter.py`: منطق التجزئة وإخراج الصورة/القناع.
- `segment_organoid.py`: غلاف CLI.

## ✨ الميزات

- واجهة ويب على `http://localhost:8888` للتجزئة التفاعلية السريعة.
- نقطة نهاية شبيهة REST: `POST /api/segment` مع دعم الرفع بصيغة multipart.
- اسم نموذج قابل للضبط من الواجهة وCLI (الافتراضي `gpt-4o-2024-08-06`).
- التحقق من نقاط المضلع وتقييدها تلقائيًا ضمن حدود الصورة.
- إنشاء تلقائي لمجلدات الإخراج (`uploads/`, `outputs/`).
- اعتماد OpenAI Responses API أولًا، مع Chat Completions كمسار بديل في الكود.
- دعم service worker لتخزين الملفات الثابتة الأساسية.

## 🗂️ بنية المشروع

```text
Yinghan/
├─ organoid_segmenter.py          # منطق التجزئة الأساسي وعرض المخرجات
├─ segment_organoid.py            # نقطة دخول CLI
├─ server.py                      # خادم Tornado + API
├─ requirements.txt               # تبعيات Python
├─ templates/
│  └─ index.html                  # واجهة Web UI الأساسية
├─ static/
│  ├─ app.js                      # منطق رفع الواجهة الأمامية + عرض النتائج
│  ├─ styles.css                  # أنماط الواجهة
│  ├─ manifest.json               # ملف تعريف PWA
│  └─ sw.js                       # منطق تخزين service worker
├─ i18n/                          # ملفات README المترجمة
├─ uploads/                       # تخزين الرفع وقت التشغيل (gitignored)
├─ outputs/                       # مخرجات التجزئة وقت التشغيل (gitignored, تُنشأ وقت التشغيل)
└─ .auto-readme-work/             # سياق/مخرجات خط أنابيب توليد README
```

## ✅ المتطلبات المسبقة

- Python 3.10+ (يوصى بـ 3.11).
- `pip` ودعم البيئات الافتراضية (`venv`).
- مفتاح OpenAI API مع صلاحية الوصول إلى نموذج يدعم الرؤية.
- وصول شبكي من بيئة التشغيل إلى OpenAI APIs.

## ⚙️ التثبيت

```bash
git clone <your-repo-url>
cd Yinghan

python -m venv .venv
source .venv/bin/activate  # Windows: .venv\\Scripts\\activate

pip install -r requirements.txt
```

اضبط مفتاح API الخاص بك:

```bash
export OPENAI_API_KEY="your_api_key_here"  # Windows PowerShell: $env:OPENAI_API_KEY="your_api_key_here"
```

## 🚀 الاستخدام

### ⚡ ملخص الأوامر

| المهمة | الأمر |
|---|---|
| تشغيل خادم الويب | `python server.py` |
| تشغيل تجزئة CLI لصورة واحدة | `python segment_organoid.py /path/to/image.jpg` |
| تشغيل CLI مع نموذج ومجلد إخراج محددين | `python segment_organoid.py /path/to/image.jpg --out-dir outputs --model gpt-4o-2024-08-06` |
| استدعاء نقطة نهاية API | `curl -X POST http://localhost:8888/api/segment -F "image=@/path/to/image.jpg" -F "model=gpt-4o-2024-08-06"` |

### 🌐 تشغيل تطبيق الويب

```bash
python server.py
```

افتح:

```text
http://localhost:8888
```

تدفق الويب:
1. اختر صورة.
2. غيّر النموذج اختياريًا في حقل الإدخال.
3. انقر **Segment**.
4. راجع الـ overlay والصورة المشروحة والقناع.

### 🧪 تشغيل CLI

```bash
python segment_organoid.py /path/to/image.jpg
```

وسائط اختيارية:

```bash
python segment_organoid.py /path/to/image.jpg --out-dir outputs --model gpt-4o-2024-08-06
```

يطبع CLI مسارات الإخراج وملخصًا يتضمن أبعاد الصورة وعدد نقاط المضلع.

### 🔌 استدعاء API مباشرة

```bash
curl -X POST http://localhost:8888/api/segment \
  -F "image=@/path/to/image.jpg" \
  -F "model=gpt-4o-2024-08-06"
```

شكل استجابة نموذجي:

```json
{
  "width": 1024,
  "height": 1024,
  "polygon": [[100.0, 120.0], [110.0, 125.0]],
  "confidence": 0.92,
  "annotated_url": "/outputs/example_annotated.png",
  "mask_url": "/outputs/example_mask.png",
  "json_url": "/outputs/example_polygon.json",
  "upload_url": "/uploads/upload.jpg"
}
```

## 🛠️ الإعداد

المعلمات القابلة للضبط حاليًا:

| المعلمة | الافتراضي | مكان الضبط |
|---|---|---|
| `model` | `gpt-4o-2024-08-06` | نموذج الويب `model`، CLI `--model`، وحقل API `model` |
| `out_dir` | `outputs` | CLI `--out-dir` |
| API key | none | متغير البيئة `OPENAI_API_KEY` |

الافتراضات:
- عميل `OpenAI()` يستخدم بيانات الاعتماد من البيئة.
- لا حاجة إلى base URL مخصص أو إعدادات org/project إلا إذا كان إعداد حسابك يتطلب ذلك.

## 🧾 الأمثلة

### 🐍 استخدام Python برمجيًا

```python
from organoid_segmenter import segment_organoid

result = segment_organoid(
    image_path="sample.jpg",
    out_dir="outputs",
    model="gpt-4o-2024-08-06",
)

print(result.annotated_path)
print(result.mask_path)
print(result.json_path)
print(result.confidence)
```

### 📄 فحص Polygon JSON

```bash
cat outputs/<name>_polygon.json
```

### 🧱 ملفات الإخراج النموذجية

```text
outputs/
├─ <base>_<timestamp>_annotated.png
├─ <base>_<timestamp>_mask.png
└─ <base>_<timestamp>_polygon.json
```

## 🧠 ملاحظات التطوير

- إطار الواجهة الخلفية: Tornado (`server.py`).
- مكدس الواجهة الأمامية: HTML/CSS/JS ثابت (`templates/index.html`, `static/app.js`).
- يجري تسجيل service worker عند تحميل الصفحة ويخزن الأصول الأساسية المدرجة في `static/sw.js`.
- يتحقق النظام من المضلع بحيث يحتوي على 3 نقاط على الأقل ويقيد الإحداثيات ضمن حدود الصورة.
- توليد المخرجات يستخدم Pillow (`PIL.Image`, `ImageDraw`).

نصائح للتطوير المحلي:

```bash
# Run server
python server.py

# Run CLI against the included sample image
python segment_organoid.py 6f1e1874eacffe1dbae0393f48811e74.jpg
```

## 🩺 استكشاف الأخطاء وإصلاحها

مطابقة سريعة:

| العَرَض | السبب المرجح | فحص سريع |
|---|---|---|
| خطأ مصادقة | مفتاح API مفقود/غير صالح | `echo $OPENAI_API_KEY` في الصدفة النشطة |
| خطأ تحليل JSON أو schema | خرج النموذج غير سليم | أعد المحاولة أو بدّل النموذج في UI/CLI |
| أقل من 3 نقاط مضلع | استخراج حدود منخفض الثقة | جرّب صورة أوضح ثم أعد التشغيل |
| الواجهة تعمل لكن التجزئة تفشل | استثناء في الواجهة الخلفية أثناء استدعاء API | افحص سجلات الخادم بحثًا عن `error_type` |
| خطأ import/module | عدم تطابق البيئة | أعد تثبيت التبعيات في venv النشطة |

- `openai.AuthenticationError` (أو ما شابهه):
  - تأكد أن `OPENAI_API_KEY` مضبوط في نفس جلسة الصدفة.
- `Model response did not contain valid JSON`:
  - أعد المحاولة أو استخدم نموذجًا مختلفًا؛ يوجد تحليل بديل لكن الخرج المشوّه قد يفشل.
- `Polygon must contain at least 3 points`:
  - خرج النموذج غير صالح؛ أعد المحاولة بصورة أوضح وذات تباين أعلى.
- الواجهة تُحمّل لكن التجزئة تفشل:
  - افحص سجلات الخادم بحثًا عن `error_type` وتفاصيل stack trace من `/api/segment`.
- `ModuleNotFoundError`:
  - أعد تثبيت التبعيات في البيئة الافتراضية النشطة باستخدام `pip install -r requirements.txt`.

## 🛣️ خريطة الطريق

الخطوات المحتملة التالية لهذا المستودع:

1. إضافة اختبارات آلية للتحقق من المضلع وتوليد المخرجات.
2. إضافة CI (lint، وفحوصات النوع، واختبارات smoke).
3. إضافة وضع CLI دفعي لمعالجة المجلدات.
4. دعم أقنعة متعددة للكائنات أو مخرجات تجزئة كائنية (instance segmentation).
5. إضافة Dockerfile ووثائق النشر.
6. إضافة أمثلة قياس أداء ومجموعات بيانات تجريبية مع مخرجات متوقعة.
7. إكمال ملفات README متعددة اللغات تحت `i18n/`.

## 🤝 المساهمة

المساهمات مرحب بها.

سير عمل مقترح:

1. اعمل Fork للمستودع وأنشئ فرع ميزة.
2. نفّذ تغييرات مركزة برسائل commit واضحة.
3. تحقّق محليًا من تدفقات الويب وCLI يدويًا.
4. افتح pull request يوضح تغييرات السلوك وأدلة الاختبار.

مجالات مساهمة مقترحة:
- تحسين تصميم الموجه لثبات أفضل في استخراج المضلع.
- تحسين التصور في الواجهة الأمامية (تكبير/تحريك، تنعيم الحدود).
- أدوات اختبار وعينات قابلة لإعادة الإنتاج.
- تحسينات التوثيق والتوطين.

<a id="support"></a>

## ❤️ Support

| Donate | PayPal | Stripe |
|---|---|---|
| [![Donate](https://img.shields.io/badge/Donate-LazyingArt-0EA5E9?style=for-the-badge&logo=ko-fi&logoColor=white)](https://chat.lazying.art/donate) | [![PayPal](https://img.shields.io/badge/PayPal-RongzhouChen-00457C?style=for-the-badge&logo=paypal&logoColor=white)](https://paypal.me/RongzhouChen) | [![Stripe](https://img.shields.io/badge/Stripe-Donate-635BFF?style=for-the-badge&logo=stripe&logoColor=white)](https://buy.stripe.com/aFadR8gIaflgfQV6T4fw400) |

<a id="license"></a>

## 📄 الترخيص

لا يوجد حاليًا ملف ترخيص في هذا المستودع.

الافتراض: جميع الحقوق محفوظة افتراضيًا إلى أن يُضاف ترخيص بشكل صريح.

إذا كنت تخطط لمشاركة هذا المشروع أو توزيعه، أضف ملف `LICENSE` وحدّث هذا القسم.
